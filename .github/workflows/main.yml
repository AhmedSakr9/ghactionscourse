name: first workflow 
on: 
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome message
        run: echo " Hello to the first workflow!"

      - name: checkout repository 
        uses: actions/checkout@v5

      - name: list all files 
        run: ls -la 

      
  
